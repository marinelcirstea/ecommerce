// let's keep it the same as product config
const categoryConfig = {
  CATEGORY_TITLE_MIN_LENGTH: 1,
  CATEGORY_TITLE_MAX_LENGTH: 150,
  CATEGORY_DESCRIPTION_MIN_LENGTH: 1,
  CATEGORY_DESCRIPTION_MAX_LENGTH: 5000,
  CATEGORY_DEFAULT_DESCRIPTION: "This category doesn't have any description.",
};

export default categoryConfig;
